# 專案一：RedoDay — 刷題進度追蹤系統

（名字自己改，我先取一個好稱呼它）

技術棧：**Java + Spring Boot + Thymeleaf + PostgreSQL**
時程：2026-08-07 ～ 2026-09-20，對齊 `SUMMER_SCHEDULE.md` 六週
定位：**我自己每天會用的工具**，不是履歷用的 demo

---

## 這個專案在解決什麼問題（面試會問，先想清楚）

我用 markdown 記刷題 LOG，三個問題：

1. **重做日期會忘。** 記在文字裡，沒有人提醒我今天有哪幾題到期
2. **看不出趨勢。** 守則說「要盯的是 L3/L4 比例往下掉」，但 markdown 算不出比例
3. **翻找很慢。** 想知道「我在哪些題卡過 helper 簽名」得整份搜尋

所以要做的不是「又一個 CRUD todo list」，是**針對這三件事的工具**。
面試時這段就是你的開場——不是「我做了一個網站」，是「我遇到什麼問題、為什麼現成工具不夠用」。

---

## 功能範圍

### v0（MVP，第 2～3 週要完成）

- 新增一筆刷題紀錄
- 列表顯示全部紀錄，最新在上
- 編輯、刪除
- **首頁顯示「今天到期的重做」**

一筆紀錄要有什麼欄位，**你自己列**。至少要能表達現在 LOG.md 那一行的所有資訊。
列完貼給我，我只檢查有沒有漏、型別合不合理，不幫你設計。

### v1（第 4～5 週）

- 重做日期**自動算**：L0 不排、L1 以上 7 天後
- 標記「重做完成」→ 產生新紀錄，並跟原本那筆關聯起來（同一題的歷史看得到）
- 篩選與搜尋：依等級、依關鍵字找卡點
- 統計：每週 L3/L4 佔比、連續記錄天數

### v2（第 6 週，做得完才做）

- 匯入現有的 `LOG.md`（解析那些反引號的行）
- 趨勢圖表
- 每日提醒（email 或 LINE）

**v2 沒做完不影響專案完整性。v0 + v1 + 上線 + README 就是一個完整作品。**

---

## 六週里程碑

| 週 | 目標 | 完成的定義 |
|---|---|---|
| 1（8/07-8/13） | 環境 + 骨架 + 資料模型 | `mvn spring-boot:run` 跑得起來，瀏覽器看到一個你寫的頁面。資料表設計圖畫出來 |
| 2（8/14-8/20） | 後端 CRUD | 能新增、查詢、修改、刪除，資料真的進得了資料庫 |
| 3（8/21-8/27） | Thymeleaf 畫面 | 從瀏覽器操作完整流程，醜沒關係 |
| 4（8/28-9/03） | **部署上線** | 手機打開網址，它是活的。資料庫換成 PostgreSQL |
| 5（9/04-9/10） | v1 功能 + 回饋修正 | 自動算重做日、到期清單、統計。找三個人用過 |
| 6（9/11-9/20） | 收尾 | README 四段寫完、Git 歷史乾淨、能講 20 分鐘 |

**第 4 週是分水嶺。** 第 3 週結束時就算功能很陽春也要先部署一次，不要等做完才部署。

---

## 第 1 週具體任務（照順序做）

1. 裝好 JDK 21 + Maven，確認 `java -version`、`mvn -version` 有反應
2. 用 [start.spring.io](https://start.spring.io) 產生專案，勾選：Spring Web、Spring Data JPA、Thymeleaf、H2 Database、Validation
3. 跑起來，做一個 `/hello` 頁面顯示你自己寫的字
4. **建 Git repo，第一個 commit**。之後每天至少一個 commit
5. 資料模型設計：拿紙畫。一題紀錄有哪些欄位、型別是什麼、哪些可以是 null
6. 寫第一個 Entity + Repository，用 H2 存一筆假資料進去，證明存得進去

第 6 步做完這週就成功了。功能一個都沒有沒關係。

---

## AI 使用規則（這是重點，不然這個專案白做）

刷題有 L0～L4，專案也要有界線。目的一樣：**手打過的才是你的。**

### 可以問，不算作弊

- Java／Spring 的語法和 API 怎麼用
- 錯誤訊息看不懂：**貼完整 stack trace 問「這在說什麼」**
- 環境、設定檔、部署平台怎麼操作
- 名詞解釋：什麼是 Bean、什麼是 ORM、@Transactional 在幹嘛
- **Code review**：「這是我寫的，有什麼問題」← 多用這個
- 「我打算這樣設計，有什麼坑」← 講你的方案，讓 AI 挑毛病

### 不可以

- 「幫我寫一個 XXX 功能」
- 把 AI 生成的整段 code 複製貼上
- 沒看懂就先貼上去跑跑看

### 唯一一條硬規則

> **任何進到專案裡的 code，我要能逐行解釋它在幹嘛。**
> 解釋不出來就刪掉重寫。

檢驗方式：隨機挑一個你昨天寫的檔案，講給我聽。講不出來的部分就是你複製貼上的部分。

### 卡住時的順序

```
1. 讀錯誤訊息（真的讀完，不要只看第一行）
2. 站起來 30 秒
3. 寫下「我卡在：____」
4. 查官方文件
5. 問 AI「這個錯誤在說什麼」（不是「幫我修」）
6. 還是不行 → 問 AI 給方向，但 code 自己打
```

---

## 每日開發紀錄

專案 repo 裡開一個 `DEV_LOG.md`，每天結束寫三行：

```
2026-08-07
做了：建好專案骨架，/hello 頁面跑起來
卡住：Maven 一直抓不到套件，原因是公司網路 proxy（花 40 分鐘）
明天：畫資料表設計圖
```

這份紀錄有兩個用途：面試講「遇過什麼 bug 怎麼修」時直接翻出來；還有讓你看見自己確實在前進。

---

## 驗收標準（9/20 檢查）

- [ ] 有網址，手機打得開
- [ ] 我自己真的用它記了至少兩週的刷題紀錄
- [ ] 至少三個人用過並給了意見
- [ ] README 四段齊全：解決什麼問題、技術選型為什麼、遇過什麼 bug 怎麼修、下一步
- [ ] Git 歷史看得出開發過程（不是一次上傳完成品）
- [ ] 我能逐行解釋專案裡任何一個檔案
- [ ] 能對非技術的人講清楚它在幹嘛，三分鐘

**最後兩項最重要。** 前面五項是別人看得到的，後面兩項決定面試會不會過。
