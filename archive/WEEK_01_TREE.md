# 第一週：樹（07/26 – 08/01）

已完成：94, 100, 101, 104, 108, 110, 226

---

## 每天的時間結構

**新題排進藥效窗口，窗口外只做低負荷的事。**

| 區塊 | 內容 | 時間 | 負荷 |
|---|---|---|---|
| A | **新題 2 題** | ~2.5h | 高 ← 必須在藥效窗口內 |
| B | **重做 1 題**（做過的，不看 code） | ~30min | 中 |
| C | **ReviewVariants 變體題 1 題** | ~1h | 中 |
| D | **寫 pattern 筆記 + 記 LOG** | ~30min | 低 |

實際工作約 4.5 小時，加休息大約填滿 6 小時。

**每 50 分鐘起來走 5 分鐘。** 不是建議，是排進去的。連續坐 3 小時之後你的產出是負的。

---

## 每題的流程

```
1. 填三格（見 LEARNING_RULES.md），不准先碰鍵盤
2. 計時 8 分鐘，寫第一版（爛的也算）
3. 卡住 → 站起來 → 畫圖手算 → 還卡就領 L1
4. 記一行 LOG
```

**今天最低標準：新題 1 題 + 記一行 log。**

達到最低標準今天就算過。剩下的是加分。

---

## Day 0 — 07/26（日）｜基準測驗日．不做新題

前面的樹題都是跟著 AI 一步一步走完的。**現在測一次，看哪幾題是真的我的。**

檔案已經開好了，在 `redo/` 資料夾，六個空的：

```
94  binary-tree-inorder-traversal
100 same-tree
101 symmetric-tree
104 maximum-depth
108 sorted-array-to-bst
110 balanced-binary-tree
```

**規則（這是測驗，不是練習）：**

```
□ 不看舊 code
□ 不領任何提示，一級都不行
□ 每題 15 分鐘上限，時間到就換下一題
□ 每題先填三格再碰鍵盤
□ 做不出來就記 N，直接下一題，不要糾結
```

六題大約 2 小時。做完在檔案頂端記下 Y / N 和卡點。

**做不出來不是失敗，是這一天的產出。** 測出來的空洞就是下週要補的清單。
如果六題全部做不出來，那也是資訊 —— 表示前面那幾題根本沒學進去，那本身就值得知道。

做完統計：

```
Y = ___ / 6
```

---

## Day 1 — 07/27（一）

**新題：111. Minimum Depth｜112. Path Sum**

- `111` 跟 104 幾乎一樣，但有一個地方不能照抄。你很可能會寫錯 —— 那是這題的重點
- `112` 第一次「把資訊往下傳」，而不是把答案往上回傳。第 3 格會多一個參數

**重做：** 昨天測驗記 N 的題目挑一題
**變體：** `ReviewVariants/Review01TwoSumPairCount.java`

---

## Day 2 — 07/28（二）

**新題：543. Diameter of Binary Tree｜572. Subtree of Another Tree**

- `543` **本週最重要的一題。** 它逼你發現「遞迴回傳的東西」和「題目要的答案」是兩個不同的東西。卡到 L2 很合理
- `572` 遞迴裡呼叫另一個遞迴。你已經有 100. Same Tree 了

**重做：** 110（你當時卡在「回傳高度還是布林」，現在應該不一樣了）
**變體：** `Review09PalindromeWithoutString.java`

---

## Day 3 — 07/29（三）

**新題：235. LCA of BST｜700. Search in a BST｜701. Insert into a BST**

三題都很短，主題一致：**開始利用 BST「左邊都比較小」的性質。**

不用這個性質也寫得出來，但會很痛苦 —— 痛苦本身就是這組題要教你的事。

**重做：** 543
**變體：** `Review13RomanLowercase.java`

---

## Day 4 — 07/30（四）｜緩衝日

**不排新題。**

1. 前三天有沒有哪天沒做完？今天補
2. 全部完成的話：不看 code 重寫 111、543、572
3. 有餘力再做 `617. Merge Two Binary Trees`（輕鬆題）

**排定的休息日不是偷懶。** 它是為了讓「漏掉一天」有地方被吸收，不會演變成整套放棄。

---

## Day 5 — 07/31（五）

**新題：102. Level Order Traversal｜199. Binary Tree Right Side View**

**換工具的一天。** 前面全部是遞迴（DFS），這兩題用 BFS，你會需要一個 `Queue`。

`102` 是 Medium 但機械性強，做過一次就會了。`199` 是 `102` 的變形 —— 連著做，感受一下「同一個 pattern 的變形」是什麼感覺。

**重做：** 112
**變體：** `Review21MergeSortedListsUnique.java`

---

## Day 6 — 08/01（六）

**新題：98. Validate BST｜230. Kth Smallest in a BST**

- `98` 經典的「把範圍傳下去」。**這題有一個陷阱：只比較父子節點是不夠的。** 先想清楚為什麼
- `230` 用到 inorder traversal（你做過 94）的一個性質

**重做：** 102
**變體：** `Review26RemoveDuplicatesAtMostTwo.java`

---

## 下週再碰

`236. LCA of Binary Tree`、`105. Build Tree from Preorder + Inorder`

原本排在本週最後一天，但加了基準測驗日之後排太滿。這兩題難度跳很多，狀態不好的時候做只會挫折，沒有學習效果。

---

## 週日（08/02）檢查

打開 LOG.md，數這一週：

```
L0 + L1 = ___ 題
L2      = ___ 題
L3 + L4 = ___ 題
```

**只看這個。不看做了幾題。**

另外回答兩題：

> 這一週我有幾天是「想到了但沒寫下來」？
>
> 6 小時的排程實際跑起來如何？撐不撐得住？

第二題很重要 —— 如果第三天就崩了，那是排程的問題，不是你的問題，下週要調整。

---

## 這一週不做的事

- 不碰 95、96（Catalan / DP，還太早）
- 不換主題
- 不重建資料夾結構、不換工具、不優化流程
- 不看完整課程。缺什麼查什麼，30 分鐘內回來做題
