/*
變體題：Remove Duplicates From Sorted Array At Most Two

你做過原題：26. Remove Duplicates from Sorted Array

題目：
給你一個由小到大排序的整數陣列 nums。
請在原陣列中移除多餘重複元素，讓每個數字最多出現兩次。
最後回傳保留下來的長度 k。

規則：
1. 必須在 nums 原地修改。
2. LeetCode 只檢查 nums 前 k 個元素。
3. 每個不同數字最多可以出現兩次。

範例：
nums = [1, 1, 1, 2, 2, 3]
處理後前 k 個可以是 [1, 1, 2, 2, 3]
return 5

練習重點：
- slow pointer / k
- sorted array 的特性
- 判斷目前數字是否可以保留
*/
class Review26RemoveDuplicatesAtMostTwo {
    public int removeDuplicates(int[] nums) {
        return 0;
    }
}
