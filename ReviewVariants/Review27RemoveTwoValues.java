/*
變體題：Remove Two Values

你做過原題：27. Remove Element

題目：
給你一個整數陣列 nums，以及兩個要移除的值 val1 和 val2。
請在原陣列中移除所有等於 val1 或 val2 的元素。
最後回傳保留下來的長度 k。

規則：
1. 必須在 nums 原地修改。
2. LeetCode 只檢查 nums 前 k 個元素。
3. 前 k 個元素的順序要和原本相同。

範例：
nums = [3, 2, 2, 3, 4, 2], val1 = 3, val2 = 2
處理後前 k 個是 [4]
return 1

練習重點：
- j 掃描所有元素
- k 記錄下一個保留元素要放的位置
- if 條件使用 && 判斷不是 val1 也不是 val2
*/
class Review27RemoveTwoValues {
    public int removeTwoValues(int[] nums, int val1, int val2) {
        return 0;
    }
}
